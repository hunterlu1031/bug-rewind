/** Max auto-captured screenshots per recording session. */
export const MAX_SCREENSHOTS_PER_RECORDING = 15;

/** Debounce duplicate captures on the same element/key. */
export const SCREENSHOT_DEBOUNCE_MS = 800;
